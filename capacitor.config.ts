import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lyx',
  appName: 'LYX',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
